/* Enter your solutions in this file */
#include <stdio.h>
int max(int[] , int ){
  int a,highest=0;
  for(a=0;a<int;a++){
    if(int[a] > highest )
      highest = int[a];
  }
  return highest;
}
    
  
